export function CompleteRefresh(element) {
    element.complete();
}

export function CancelRefresh(element) {
    element.cancel();
}

export function RefreshGetProgress(element) {
    return element.getProgress();
}